node_modules/
dist/
*.log
data/*.db
data/*.db-*
data/uploads/
.env
test-results/
playwright-report/
docs/review-pack/
.vite/
.claude/

.vercel
# `vercel link` appends a blanket `.env*` here, which also hides .env.example —
# the one env file that is meant to be committed. Enumerate instead.
.env.local
.env.*.local
.playwright-mcp/

# Agent scratch/debug scripts — never part of the app
__*.mjs
