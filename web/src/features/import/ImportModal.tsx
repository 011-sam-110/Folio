// STUB — web-study-import replaces this (keep the prop signature: it is imported
// by the shell and the editor at this exact path).
export interface ImportModalProps {
  open: boolean;
  onClose: () => void;
  notebookId?: string;
  noteId?: string;
  defaultKind?: 'photo' | 'slides' | 'transcript';
}

export default function ImportModal(_props: ImportModalProps) {
  return null;
}
